apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-user-pv
  namespace: microservices
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/mongo-user"